# ── HTTP service ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0
PyJWT>=2.8

# ── Persistence (users, organizations, revoked-token blacklist) ──────────────
motor>=3.3
pymongo>=4.6

# ── Password hashing (bcrypt for new hashes + imported-hash verification) ────
bcrypt>=4.2.0
